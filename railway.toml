[build]
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "npx prisma migrate deploy && node -r tsconfig-paths/register dist/src/main"

restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
